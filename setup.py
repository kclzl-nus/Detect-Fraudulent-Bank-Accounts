from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Detecting fraudent t bank accounts and gain insights into characteristics of these fraudent accounts and generate actionable insights to detect and prevent these kind of fraud.',
    author='Kyler Cheong',
    license='MIT',
)
